config:
  goTemplateRendering:
    allowEnvVariables:
    - NOCACHE
    - CI_JOB_STARTED_AT
  stapel:
    mount:
      allowBuildDir: true
giterminismConfigVersion: 1
helm:
  allowUncommittedFiles:
    - .helm/Chart.lock
    - .helm/charts/helm-snippets-0.0.1.tgz
